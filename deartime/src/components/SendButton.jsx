const SendButton = () => {
    return (
        <button style={{
        padding: '10px 20px',
        borderRadius: '8px',
        background: 'transparent',
        border: '1px solid white',
        color: 'white',
        cursor: 'pointer'
        }}>
        편지 보내기
        </button>
    );
};

export default SendButton;